Create a webpage with a 16x16 grid of square divs

<div id=container || class=grid-container>
</div>

create a 16x16 

grid
 
of square divs

CSS GRID:
<style>

.grid-container {
  display: grid;
  grid-template-columns: auto auto auto;
  background-color: #2196F3;
  padding: 10px;
  align-content: (vertically)
  justify-content: (horizontally)
}

</stlye>
let container = document.createElement('div')

let div = document.createElement('div') 
div.textContent = "";
document.body.appendChild(div);
div.style.

things to do:

make the hover only apply to the inner divs and not the container div
make the hover stay permament {
	add an eventlistener that listens to the hover effect, and when it is active it completely
	
}

div:hover {
                background-color: #ff9b9b;
            }

then a promt appears asking for a number
then it appears again asking for a second number
// 2 prompts 
// The two numbers are stored in variables and used in a new style ????
// Button when pressed which will clear the current grid
// and send the user a popup asking for how many squares per side 
// to make the new grid. 
// Once entered the new grid should be generated in the same total space as before (e.g. 960px wide) and now you’ve got a new sketch pad.



prompt number
if (prompt > 1 || promt < 100 ) {
	let promted number = num
	calcSize
} else { 
	error (num or string)
	

800 / 100 = result
number * result = 800
loop in now number

800 / number = result
number * result = 800

 
Set the limit for the user input to a maximum of 100. 

